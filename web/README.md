# HR Factory (Web)

This is the React (Vite) web application for HR Factory.

## Run Locally

From the repository root:

```bash
cd web
npm install
npm run dev
```

Open:

- `http://localhost:5173/`

For more details and troubleshooting, see `../LOCALSERVER.md`.
